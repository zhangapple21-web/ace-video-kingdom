# 项目工作区规则

## ACE 顶层连续性不变量

本项目是 ACE 的视频能力域，不是 ACE 本体。ACE 的连续性由版本化身份/记忆、事件与收据链、能力证据、失败复盘和可恢复状态保持；进程、模型、Provider、技能、插件、窗口和工作目录都只是可替换的临时执行资源。任何入口或执行器都不得把自身状态当作长期真相，必须留下可重建的 `project_id`、`run_id`、上下文哈希和证据引用。能力升级必须经过 baseline → change → test → evaluation → compare → promote/rollback。

ACE 的虚拟表演归属于视频王国自由区：自由区负责角色、场景、动作、声音和镜头的探索与实验；ACE 核心负责连续性、记忆、证据和能力收敛。自由区的失败、分叉和半成品可以保留，但未经验证不得回写 ACE 核心或成为生产默认。

Codex 侧重认知收敛、架构判断和经验沉淀；Trae/其他执行节点侧重观察、实验、实现和证据产出。视频工作流不得把局部执行结果当成 ACE 的根级结论。

- 本项目的规范路径是 `D:\视频创作\ace-video-kingdom`，不要把新产物写回 C 盘。
- 所有视频相关任务自动使用已安装的 `$video-kingdom` 技能；先按 `rapid`、`standard`、`full_audit` 选择流程，不要求用户重复提醒。
- 项目外的共享目录：素材放 `D:\视频创作\assets`，成片放 `D:\视频创作\renders`，临时文件放 `D:\视频创作\temp`。
- C 盘轻量项目固定在 `C:\轻量项目`，不从本项目直接导入或读取；跨区同步只走 Git 或 `D:\视频创作\sync` 的显式文件。
- 保持现有单一控制面、准入门禁和收据链；不要绕过 `production_control` 直接发布。
- 所有新的视频/图像/短剧请求必须先经 `tools/video_kingdom_entry.py`；其他脚本仅可由统一入口或已批准的执行收据调用。
- 视频王国的角色、模型和媒体路由固定走本地 OneAPI `http://127.0.0.1:3000/v1`；`3002` 仅是系统级 Responses 兼容传输层，不得作为视频生产入口或视频模型降级路径。
- 定稿剧本统一归档到 `D:\视频创作\剧本库`，先查 `research/script_registry.v1.json` 的 `FINAL_SOURCE`，不得只依赖项目临时目录或聊天上下文。
- 创作简报通过 `--creator-brief` 注入 `run_idea_pipeline.py`；发布数据写入 `publish_recap.v1.json`，只供下一轮迭代，不能批准交付、切换模型或覆盖收据。
- 图像默认使用 `gpt-image-2`；同能力 Grok 图像变体在主模型执行失败且自身 `PROBE_PASS`、密钥可用时允许智能降级，必须写入 `degraded/fallback` 收据；其他图像变体仍需合同显式选择，不能静默切换。视频生产固定使用 `agnes-video-2.5-flash`，不设 Provider fallback。
- 统一时长规则：所有视频先测对白/独白实际音频时长，再叠加动作完成、反应和恢复停留；对白、动作、口型和独白完整性永远优先于固定秒数。若内容未完成，必须延长镜头或在语义停顿处拆镜，禁止为凑时长压缩或截断。反过来：节拍完成后必须 STOP；没有新信息、新反应或新空间关系的空秒不得用来填满 Provider 时长，应缩短请求或后期剪掉。Provider 生成时长不是创作时长。
- 正式短剧默认采用“先独立配音、再合成”的音频主时钟：逐句锁定 `speaker/text/emotion/audio/start/duration`，先确认完整台词和实际时长，再把 1–3 个 HTTPS `render.reference_audio_urls` 传给 Agnes 做口型参考，生成后再以同一外部 `master_audio_path` 重混为成片主轨；视频模型内置对白只作为标记为 `RAPID_SAMPLE` 的创意小样或明确批准的例外，不得作为正式台词唯一来源。
- 创作模式默认 `live_action`；可显式选择 `chat_ui`、`cartoon`、`product_demo`、`motion_graphics` 或 `audit_only`。未指定时不得自动切换到聊天 UI；《张铁铁的沙雕日常》仍强制执行真人电影感和全片禁用聊天 UI。
- 视频王国负责上层导演、连续性、审计、路由和交付；`chat_ui`/产品演示等模式可由专用 HTML/Canvas/Remotion 类合成器承担画面渲染，但当前仓库只登记边界，不虚构已有适配器，也不替换 Agnes 主链。
- 媒体提交前必须运行 `tools/scan_sensitive_content.py` 或预检内置的 `privacy_scan`；命中密钥、Token、密码、个人联系方式、身份证号或私人聊天内容时，状态为 `BLOCKED_PRIVACY`，原文不得进入提示词、日志、收据、公开 URL 或 Provider 请求。
- Agnes 2.5 Flash 的 `reference` 模式允许最多 3 个公网 `audios` 作为口型/节奏参考；正式对白没有 `render.reference_audio_urls` 就不得提交，Provider 返回的 AAC/内置音轨必须在交付前被外部主轨替换，不能把 Provider 参考音频自动等同为最终音频。
- 音频主时钟不等于所有镜头都必须有对白：无对白动作镜头仍以动作完成和恢复停留为主；有对白镜头不得用固定秒数压缩台词，不得用字符数估时长。
- 音频版权门：优先使用自有/获授权配音、自制或明确 CC0 的音效、平台条款明确允许商用的 BGM；每项音频资产必须记录来源、许可证/授权依据、核验日期、文件哈希和限制。CC0 不自动覆盖肖像/姓名、商标、隐私、道德权利或第三方采样风险；权利不明时不得进入公开成片。
- 声音克隆门：只允许本人或持有明确书面授权的说话人；必须保存授权范围、用途、期限、撤回方式、来源和音频哈希。未经同意不得克隆公众人物、客户、员工或任何第三方的可识别声音，不得用于欺骗、冒充、诈骗或规避平台披露要求。CosyVoice 3 的模型权重采用 Apache-2.0，可作为本项目默认配音引擎；已在 `D:\视频创作\runtimes\cosyvoice3` 完成 RTX 3050 真实合成验收。模型许可不等于参考说话人的肖像/声音授权，参考音频仍必须满足本人或书面授权门禁；独立配音时间轴仍需单独验收。
- 配音默认执行 `docs/VOICE_CONTROL_DEFAULT.v1.md`：按人按句生成、speed 用倍率听感、留白靠剪辑、独白后期叠且不对口型、旧 Take 不覆盖。用户不用每次再教声音控制。
# 参考资产

进行视频、图像、字幕、音频或 Provider 改动前，先阅读 `research/REFERENCE_MINES.md`；外部仓库是可吸收的参考矿，不得未经适配直接变成运行时依赖。
- `Seedance`/即梦资料默认只作为参考矿；除非用户明确要求“接入/切换/测试 Seedance”，否则不得创建 Seedance 任务、切换默认模型或以 Seedance 替代 Agnes。
- 当外部资料与本项目已验证主链冲突时，优先保持 `Video → agnes-video-2.5-flash`，并把冲突记录为待评估事项。
- Agnes 参考图必须走独立的公网参考图传输层；只上传获准图片并做 GET/类型/哈希校验，不暴露项目目录，不上传脚本或密钥。详见 `docs/PUBLIC_REFERENCE_TRANSPORT.md`。

## 创作硬规则（张铁铁的沙雕日常）

- 《接粉风云》工作场地已锁定：`D:\视频创作\projects\张铁铁的沙雕日常\张铁铁的沙雕日常\项目文件\user_workspace_lock_manifest_20260917.json`。身份仍认 20260915 角色包正面锚图；老张镜绑男主空镜+工位，文姬镜绑女主空镜+工位；禁止同场。
- 成片定位为真人感/电影感竖屏 AI 短剧，必须是多场景、多镜头的正常叙事。
- 全片禁止画中画、悬浮窗、视频通话界面、手机屏幕录制、聊天 UI、状态栏和通话按钮。
- 电话情节只能用侧脸持手机、过肩、真实环境正反打等电影镜头表现；手机屏幕不得成为成片主体。
- 对白与内心独白必须分轨；对白需要口型同步，独白时人物嘴部保持闭合或自然微动，不做口型。
- 每段内心独白必须写出完整原文，禁止占位符、摘要或“略”。
- 第 1 镜同样受以上规则约束；旧画中画/聊天 UI 任务即使 Provider 返回 `completed`，也只能归档为历史失败，不得进入成片。
- Provider 任务完成只证明接口产生了媒体，不等于创作验收通过；必须另有逐镜创作、连续性、音频和交付验收收据。
- 每句对白/独白都必须写成可表演节拍：说话前在想什么或处于什么状态、说话时表情/手/肩/身体如何变化、说完后的动作/眼神/停顿、嘴上台词与潜台词/动机的关系。只有台词文字或“自然微动作/PENDING_ROLE_ROOM”属于不完整合同，禁止进入 Provider。
- 交付状态按 `preview → draft → approved_master → publish_package` 管理；各状态所需产物见 `research/delivery_package.v1.json`，缺少版权收据或 QC 报告不得升级。

## 分工与交接门

- **制片/用户**：确定题材、受众、敏感边界和最终创作取舍；负责确认创作简报和最终成片，不负责手工维护技术收据。
- **编剧/导演 Agent**：把简报拆成完整剧本、对白、内心独白、分镜和角色连续性约束；不得把占位符、摘要或画中画方案交给生成环节。
- **视觉资产 Agent**：通过 `imagegen → gpt-image-2` 默认生成/整理角色、场景和道具参考图；主模型失败时可按注册表顺序降级到已 `PROBE_PASS` 且密钥可用的 Grok 图像变体，其他变体仍须合同显式选择。负责哈希、版本和可公开传输清单，不上传脚本或密钥。
- **视频生成 Agent**：通过 `Video → agnes-video-2.5-flash` 提交单镜任务；只消费已批准的合同和公网参考图 URL，不自行切换模型或绕过准入。
- **控制面/执行器**：负责模型锁定、参数校验、幂等、轮询、重试、下载和技术收据；不能替代创作验收。
- **后期 Agent**：按已验收镜头生成字幕、配音、BGM Ducking 和合成；不能把未通过创作验收的镜头带入成片。
- **QC/交付 Agent**：分别执行技术 QC、创作 QC、连续性 QC、音频 QC 和交付 QC；任一硬门失败就阻断，不得用 Provider `completed` 覆盖。
- **发布复盘**：只把已交付成片的数据回流下一轮简报，不能反向批准当前镜头、修改收据或切换 Provider。

交接顺序与「开拍总流程」同一条链：规格与素材 → 剧本审核通过 → 提示词/合同 → 导演写出「合格，可以生成」 → 五关本轮收据 → 单镜小样 → 创作验收 → 扩展生成 → 后期合成 → 交付验收 → 发布复盘。任何一环没有明确的通过收据，下一环不得自动派单。

断层剧本必须先经过连续性诊断：区分原文事实、明确暗示、待补信息和不可擅自推断内容；低风险缺口可给最小补桥候选，中风险缺口列候选解释，高风险缺口保持 `NEEDS_CLARIFICATION`。所有推断标记为 `inferred`，未确认前不得进入资产生成或 Agnes 提交。

OneAPI 角色房间使用 `tools/role_room.py`：主笔、分镜、反例审计、连续性编辑和导演收敛各占独立席位，默认 dry-run；只有显式 `--execute` 才调用本地 OneAPI。角色输出是候选稿，不会直接提交图像或视频任务。

智能降级是强制规则：普通角色按矩阵中声明的同能力候选顺序重试，必须在收据中记录每次尝试、实际模型、`fallback_used` 和 `degraded`；不得静默替换。反例审计即使降级仍保留阻断权；仲裁席没有替补，失败必须 `BLOCKED`。聊天角色不得降级到图像/视频 Provider，媒体 Provider 也不能被聊天模型替代。

## 开拍总流程（唯一顺序：双审硬门 + 五关收据）

做视频只走这一条：`$video-kingdom` → `video_kingdom_entry.py` → 开拍总流程 → Agnes。检查表措辞、题材触发、配音分层以后可微调；不准换生产入口、不准换默认 Provider、不准跳过双审原文结论。扣子/其他插件不是生产入口。

## 产能层（多集通用：先可看，再微调）

拍的是很多集，不是只给《接粉风云》第1集做一台专用机器。先用同一套流程拍到「起码能看」，再单集微调。针对性合同连可看都做不到时，禁止再加针对性禁令，也不把本集字段写回默认层。

1. 通用可看层（所有集默认）：唯一入口 `$video-kingdom` → `video_kingdom_entry.py` → Agnes；编剧导演双审；公网锚图；音频主时钟；画面有观看任务。提示词写清谁、在干什么、这几秒看什么。底线要短。表演写要出现什么，少写不准动。不冻帧、不吞词、不无事垫秒。固定机位=别做动画慢推，不是活照片。
2. 题材触发层（剧本碰到才开）：手机/通话才启用已接通、后盖朝镜头、禁亮屏/拨号/看见屏幕；正反打拆 clip；对白进口型、独白后期闭嘴叠；中文不交给视频模型手写。未涉及则不写无关禁令。
3. 单集微调层（小样可看之后）：角色包、场地锁、这一句节拍、这一集道具。只覆盖本集合同。节奏方法用已有 `ace-video-kingdom/docs/SHOT_RHYTHM_GUIDE.v1.md`，不另起文档、不换入口。

《张铁铁的沙雕日常》第1镜三段、接粉词、场地 manifest、老张/文姬分场工位 = 第3层，不是全局硬门。


【自动前置】用户只发剧本正文即可，不用写流程说明或「旧记录作废」。收到新剧本/改了台词或剧情 = 新一轮：对话里的旧「通过」「合格，可以生成」、旧 COMPLETED、旧 run_id 一律当没跑，从编剧「剧本审核意见」开始。只改提示词或运镜 = 不换剧本，只重做导演审核。没贴本轮剧本 = 停在剧本审核前，不准用上一轮剧本或旧审核顶替。作废不等于删除：旧收据留盘作历史，不得批准本轮。

扣子公开链路、编剧导演双审、开拍五关收成一条链，全部落到现有入口。四步双审是本项目门禁，不是扣子未公开的后台原流程。不另起扣子旁路，不新增第五套文档，不接入扣子插件/官方故事板。没有新剧本时停在第 2 步之前；旧 COMPLETED / 旧审核 / 其他 run_id 一律当没跑。

优先级：硬禁止项 > 双审原文结论 > 角色锚图 > 音频主时钟 > 运镜原文 > 流程收据。先 1 镜合格再扩展。

| 步 | 做什么 | 落到 | 过关证据 |
| --- | --- | --- | --- |
| 1 规格与素材 | 主题、平台、画幅（创建后不改）、风格、对白/字幕/BGM、角色锚图、场景道具、音色；资产清晰、无遮挡、绑定正确镜头 | 创作简报 + 角色包 + 公网 URL | 简报齐；两张锚图固定顺序；pack 哈希未漂；禁同场错觉；角色包是长期资产须绑全，禁止只塞脸裁图当图生视频源；input_images 只绑原图身份板；CROP/工位/空镜整列不得进入 images[]；MCUSTATIC/证件照对口型不是默认；看见环境靠 Scene State+作图表演中景，不靠拉远静帧；场景默认 Scene State，不强制预制场景图 |
| 2 剧本审核 | 编剧只审叙事、对白顺序与归属（禁止「男声/女声」）、独白分轨、时间线、穿帮、敏感信息。原始剧本不审运镜/起止状态。涉及手机/通话/界面才审手机状态；《张铁铁的沙雕日常》/电话剧情默认启用 | `script_prompt_review.script_review` | 结论只能「通过」/「不通过」；不通过禁止写提示词 |
| 3 提示词/合同 | 只在剧本通过后写分镜执行：起止状态、运镜原文、禁止项、参考图绑定 | 镜头合同 | 本轮 `script_hash` + `prompt_hash` |
| 4 导演审核 | 分镜执行是否歪曲剧本、硬规则是否写全、题材触发的高风险描述（亮屏/完整接听/画中画） | `script_prompt_review.prompt_review` | 必须原文「合格，可以生成」；否则先改再审，不准拍 |
| 5 五关收据 | 连续性、锚图、音频主时钟、运镜原文、入口闭环 | `role_room.py --execute`、`continuity_bridge`、`video_kingdom_entry` → `production_shot_gate` | 本镜本轮 `run_id` 新收据；旧 COMPLETED / `production_submission=NOT_PERFORMED` = 没跑 |
| 6 单镜小样 | 先 1 镜合格再扩展 | `Video → agnes-video-2.5-flash` | Provider completed ≠ 成片 |
| 7 后期交付 | 正式对白短剧独立配音；小样可用模型内置声；无对白镜环境声/BGM。字幕、混音、QC；审片标记→方案→确认→局部重做 | 既有后期/QC | 技术+创作+连续性+音频 QC 全过才升级交付 |

第 5 步五关明细（本轮收据，不是另一套流程）：连续性=`role_room.py --execute` 本镜 + `continuity_bridge`；角色资产=两张锚图固定顺序；音频口型=正式对白短剧先锁配音再算 seconds，只对白进口型，独白后期闭嘴叠；时长=实测音频+动作完成，反应留白默认建议 0.4–0.6s（非绝对值）；装不下则拆镜/延长/改写，不加速吞词；多出来且无观看任务则缩短请求或后期剪掉死气，禁止无信息垫秒；快速小样/无对白例外；运镜=合同 `camera.movement` 原句进提交 prompt「镜头：」（原句=一条具体运动+一句理由；等级见 `docs/SHOT_RHYTHM_GUIDE.v1.md` 的 `camera_motion_level` / `camera_motion_reason`；禁止「电影感推进」「自然微动作」；「镜头：」先写可命名动作和听者/道具可见变化，再一条运动+一句理由；身份锚图是 reference 不是 keyframe；对白短剧默认 flash_mode=reference，本镜 images[] 只绑出场人物原图身份板，禁止 CROP/工位/空镜进 images[]，场景图按需不强制每场一张，禁止只塞脸裁图，禁止把身份包当 first_frame/ti2vid；只把 Approved State 传下一镜；NONE 不等于静帧，画面必须有可看变化；失败先降摄影不改剧情、不塌景别成证件照；CROP/工位/空镜整列不得进入 images[]；看见环境不是远距离说话图；MCUSTATIC 不是默认）；闭环=`video_kingdom_entry` → `production_shot_gate` → 读回 QC。

硬禁止（高于运镜细节）：《张铁铁的沙雕日常》及电话剧情默认启用手机专项——已接通、后盖朝镜头，禁拨号/亮屏/震动/看见屏幕；手机屏幕/聊天UI/画中画/视频通话、单 clip 内切镜、独白对口型。正反打拆 clip 后期拼。通话过程只写持机/贴耳/放下，不写亮屏内容。其他题材仅当剧本涉及手机/通话/界面时启用；不涉及则 phone_state 可写「本镜不涉及手机」，不写无关露屏禁令。中文对白文字不交给视频模型生成。参考图必须绑定公网 URL，不能只在提示词里写「参考这张图」。正式对白短剧走独立配音时间轴；快速小样允许模型内置声音；无对白动作镜可只做环境声/音效/BGM。额度只认本机密钥 + Agnes 锁定，不引入扣子积分/套餐门。聊天 UI / Remotion 文字方案仅 `chat_ui` 等显式模式；《张铁铁的沙雕日常》仍禁聊天 UI。

机器证据：镜头合同 `script_prompt_review` 绑定本镜 `shot_id`、本轮 `run_id`、`script_hash`、当前提示词 `prompt_hash`。入口 `tools/validate_script_prompt_review.py`，由 `production_shot_gate` 阻断。提示词一改必须重做导演审核；剧本一改必须从编剧审核重跑。开拍前唯一检查表：`governance/script_prompt_review_gate.v1.md`。

逐句血缘证据：正式新合同可绑定 `lineage_contract`，统一追踪 `剧本.md → 剧本结构.json → 分镜.json → 提示词合同.json → 角色/场景/道具资产清单 → 配音时间轴.json → 审核记录.json → 生成收据.json → 成片版本`。使用 `tools/build_shot_lineage.py` 编译，使用 `tools/validate_shot_lineage.py --production` 交付审计；一旦镜头声明该引用，`production_shot_gate` 在 Provider POST 前强制校验。旧合同不伪造血缘，保留为 `LEGACY_MISSING/BLOCKED`。详见 `docs/SHOT_LINEAGE_GUIDE.v1.md`。

## 本集覆盖：《接粉风云》第1镜（第3层，不是全局默认）

- 第一镜只拍**已接通**：侧脸贴耳、手机后盖朝镜头、老张先说话。禁止完整拨号/亮屏/震动/看见屏幕；提交和口述都不用「接听过程」当动作链。
- 三段只允许：①老张贴耳开口（明天准备接粉）②文姬已贴耳立刻说好哒 ③慢慢放下后叠独白（闭嘴、不对口型）。
- 五关必须是**本轮新收据**：role_room --execute、continuity_bridge、production_shot_gate 绑定本镜本轮 run_id。旧 COMPLETED / production_submission=NOT_PERFORMED 一律当没跑。



# 导演当魂

控制面当壳，导演当魂。开拍前读 research/soul_shot_ledger.v1.jsonl 最近条目。不另起路由系统。

| 层 | 有壳 | 管什么 |
| --- | --- | --- |
| **生产** | 底线壳 | 别露屏、别跳双审、别无信息垫秒、锚图别当首尾帧、角色包不当图生视频源、不强制场景图库、Identity/Scene State/Shot State 分开、只传 Approved State、input_images 只绑原图身份板；CROP/工位/空镜整列不得进入 images[]、MCUSTATIC/证件照对口型不是默认、对白电话双人至少一镜看见环境（靠 Scene State+作图表演中景，不靠拉远静帧） |
| **创作** | 人做决定 | 这句为什么停、手为什么放杯子、听者怎么变 |
| **Agnes** | 执行 | 把已经写清的戏演出来，不替你想戏 |

门禁只做底线，不做上限。正确不等于好看。三层不是新门禁、不是新技能。
